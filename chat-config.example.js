// Rename to `chat-config.js` (keine Secrets!).
// Setze hier NUR die Basis-URL deines Backends auf Render.
// Beispiel:
// window.__CHAT_CONFIG__ = { apiBaseUrl: "https://dein-backend.onrender.com" };
window.__CHAT_CONFIG__ = {
  apiBaseUrl: "",
};

